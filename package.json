{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "web-dev": "yarn workspace web run start",
    "api-dev": "yarn workspace api run dev",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
